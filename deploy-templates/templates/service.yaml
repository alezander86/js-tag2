apiVersion: v1
kind: Service
metadata:
  name: {{ include "js-tag2.fullname" . }}
  labels:
    {{- include "js-tag2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "js-tag2.selectorLabels" . | nindent 4 }}
